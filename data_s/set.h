#ifndef SET_H
#define SET_H

#define MAX 30
#include <stdio.h>
#include <conio.h>
#include <stdbool.h>

void addElement(char*, const char, int*);
bool member(const char*, const char, const int);

#endif